var regular_payments_confirm_payment_steps = function () { 

	this.Given(/^I am Confirm Payment screen of my Regular Payment$/, function (callback) {
    callback.pending();
  });

	this.Given(/^I am on the Confirm Payment Screen$/, function (callback) {
    callback.pending();
  });
	
	this.Then(/^I should see the following Confirm Payment elements on the page$/, function (callback) {
    callback.pending();
  });
		// | title 								|
		// |	Payment amount 				|
		// | calculated tax releif |
		// | legal summary					|
		// | back button						|
};

module.exports = regular_payments_confirm_payment_steps;