var top_up_confirm_payment_steps = function () {

	this.Given(/^I am Confirm Payment screen of my Top Up$/, function (callback) {
	  callback.pending();
	});

	this.Then(/^I should have the following working links within the page:$/, function (callback) {
	  callback.pending();
	});
		// | Terms and Conditions	|
		// | Keyfacts document 		|
		// | Key features document	|

	this.Then(/^I should see the following Confirm Payment elements on the page:$/, function (callback) {
	  callback.pending();
	});
	// | title 								|
	// |	Payment amount 				|
	// | calculated tax releif |
	// | legal summary					|
	// | back button						|

};

module.exports = top_up_confirm_payment_steps;